[build]
  command = "npm run build"
  publish = "dist"

[functions]
  directory = "netlify/functions"

# SPA fallback: semua route ke index.html
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
